Bootstrap: docker
From: nvcr.io/nvidia/pytorch:24.01-py3

%post
    pip install --no-cache-dir \
        torchvision>=0.15.0 \
        Pillow>=9.0.0 \
        PyYAML>=6.0 \
        tqdm>=4.65.0 \
        scikit-learn>=1.3.0 \
        dvc[s3]

%runscript
    exec python "$@"
